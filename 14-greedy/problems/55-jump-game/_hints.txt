---
title: "💡 Hints: 55. Jump Game"
type: hints
---

## Hint 1/2
Track the furthest reachable position.

## Hint 2/2
If current index > furthest, return false.

## Solution
```go
func canJump(nums []int) bool {
    maxReach := 0
    for i := 0; i < len(nums)-1; i++ {
        if i > maxReach { return false }
        maxReach = max(maxReach, i+nums[i])
        if maxReach >= len(nums)-1 { return true }
    }
    return true
}
```

---
**[[_prompt.md]]**

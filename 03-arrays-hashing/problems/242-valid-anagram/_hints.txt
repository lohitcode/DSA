---
title: "💡 Hints: 242. Valid Anagram"
type: hints
---

## Hint 1/2
Count characters in one string, decrement for the other.

## Hint 2/2
Or sort both strings and compare.

## Solution
```go
func isAnagram(s, t string) bool {
    if len(s) != len(t) { return false }
    count := make(map[rune]int)
    for _, c := range s { count[c]++ }
    for _, c := range t {
        count[c]--
        if count[c] < 0 { return false }
    }
    return true
}
```

---
**[[_prompt.md]]**
